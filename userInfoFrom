import javax.Swing.*;
import java.awt.*;
import java.awt.event.*;

public class userInfoFrom Extends JFrame implements ActionListner { 
Jlable nameLable,ageLable;
JTextField nameField, ageField;
JButton checkButton;
public UserInfoFrom() {
setLayout(new FlowLayout());
setSize(300,200);
setTitle("User Info Form");
setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
nameLable = new JTextField(15);
ageLable = new Jlable("Enter Age:");
ageField = new JTextField(5);
checkButton = new JButton(" Check Eligibility");
checkButton.addActionListener(this);

add(nameLable);
add(nameField);
add(ageLable);
add(ageField);
add(checkButton);

set
